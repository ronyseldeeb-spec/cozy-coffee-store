name: Apply Cozy Brews UI upgrade
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete workflow disabled"
